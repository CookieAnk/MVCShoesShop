@model IEnumerable<MVCShoesShop.Models.LOAI_SAN_PHAM>

<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"  style="font-weight:bold; font-size:15pt">CATEGORIES <span class="caret"></span></a>
    <ul class="dropdown-menu dropdowncostume">
        <li style="">
            @foreach (var item in Model)
            {
                <a href="@Url.Action("spTheoLoai", "Shop", new { id = item.MaLoaiSP })" class="list-group-item" style="font-size:15pt">
                    @item.TenLoai
                </a>
            }
        </li>
    </ul>
</li>